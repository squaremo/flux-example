---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: helloworld
  namespace: hello
spec:
  minReadySeconds: 5
  replicas: 2
  template:
    metadata:
      labels:
        name: helloworld
    spec:
      containers:
      - name: mello
        image: squaremo/helloworld:master-a000001
        ports:
        - containerPort: 80
      restartPolicy: Always
